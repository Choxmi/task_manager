# task_manager

## API

* Navigate to ./task_manager_api/src/main/resources/
* Change application.properties username and password
* cd to ./task_manager_api
* mvn clean install -DskipTests
* start api server

## UI

* Navigate to ./task_manager_ui
* npm install
* npm start
(Application should run on 3000 port to bypass CORS)